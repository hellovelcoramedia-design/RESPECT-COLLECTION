document.addEventListener('DOMContentLoaded',()=>{const h=document.querySelector('[data-header]');const m=document.querySelector('[data-menu]');const mobile=document.querySelector('[data-mobile]');const count=document.querySelector('[data-cart-count]');window.addEventListener('scroll',()=>h&&h.classList.toggle('is-scrolled',scrollY>30));m&&m.addEventListener('click',()=>mobile.classList.toggle('is-open'));const hero=document.querySelector('[data-hero]');const tilt=document.querySelector('[data-tilt]');if(hero&&tilt&&matchMedia('(pointer:fine)').matches){hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;tilt.style.transform=`perspective(1200px) rotateY(${x*10}deg) rotateX(${-y*8}deg) translate3d(${x*8}px,${y*8}px,0)`});hero.addEventListener('pointerleave',()=>tilt.style.transform='')};document.querySelectorAll('[data-add]').forEach(b=>b.addEventListener('click',async()=>{b.disabled=true;try{const r=await fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({items:[{id:Number(b.dataset.add),quantity:1}]})});if(!r.ok)throw 0;const c=await fetch('/cart.js').then(x=>x.json());if(count)count.textContent=c.item_count;b.textContent='Added ✓';setTimeout(()=>{b.textContent='Add to bag';b.disabled=false},1200)}catch(e){b.textContent='Try again';setTimeout(()=>{b.textContent='Add to bag';b.disabled=false},1200)}}))});